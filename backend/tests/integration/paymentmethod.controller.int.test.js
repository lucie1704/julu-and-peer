describe('Tests d\'intégration pour le controller des méthodes de paiements', () => {
    
    it('devrait tester le fichier d\'intégration pour les méthodes de paiements', async () => {
        expect(1).toBe(1);
    });

});
