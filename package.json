{
    "name": "julu-and-peer",
    "version": "1.0.0",
    "scripts": {
      "start": "cd backend && npm install && npm start",
      "heroku-postbuild": "cd frontend && npm install && NODE_OPTIONS='--max_old_space_size=512' && npm run build"
    },
    "engines": {
      "node": "=16.0.0"
    }
  }